# IOT-dashboard

#**Run command**
Goto backend folder open terminal then run this command.
    **.\mvnw.cmd -e spring-boot:run**
#**Frontend**
goto frontend folder run the below command
      **npm run dev**
